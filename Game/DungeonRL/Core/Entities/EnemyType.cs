/// <summary>
/// Тип врага определяет способ передвижения и уязвимости.
/// </summary>
public enum EnemyType
{
    Walking,  // ходячий  — уязвим к удару и выстрелу; движется по полу
    Flying,   // летающий — уязвим ТОЛЬКО к выстрелу;  игнорирует ямы
    Crawling  // ползающий — уязвим ТОЛЬКО к удару;    движется по полу
}
