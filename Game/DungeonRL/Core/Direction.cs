/// <summary>
/// Направление взгляда игрока (обновляется при каждом шаге движения).
/// Используется CombatSystem для определения зоны атаки/выстрела.
/// </summary>
public enum Direction
{
    Up,
    Down,
    Left,
    Right
}
